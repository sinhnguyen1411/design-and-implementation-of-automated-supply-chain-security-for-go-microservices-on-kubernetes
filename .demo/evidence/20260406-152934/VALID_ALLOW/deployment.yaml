apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: stock-trading
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: stock-trading
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: user-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: user-service
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: stock-trading
      annotations:
        kubectl.kubernetes.io/default-container: user-service
        security.grype.io/high_critical: "0"
        security.stock-trading.dev/sbom-digest: "534E61EF4AE7D989C9CF4D365E6B4FCC69006688207CD40E7EA4139B447CDB23"
        security.stock-trading.dev/matrix-case: "VALID_ALLOW"
    spec:
      serviceAccountName: user-service
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 65532
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: user-service
          image: ttl.sh/stock-trading-matrix-signed-6da392d7586f@sha256:8404ccf820241d03fee2e9c9e5d5b14ac07dc348d23a6ae7639e4172f67c076d
          imagePullPolicy: Always
          args: ["server", "--config", "/etc/stock-trading/config/config.yaml"]
          env:
            - name: AUTH__ACCESS_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: user-service-secrets
                  key: auth-access-token-secret
            - name: AUTH__REFRESH_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: user-service-secrets
                  key: auth-refresh-token-secret
          ports:
            - name: http
              containerPort: 18080
            - name: grpc
              containerPort: 19090
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 15
          startupProbe:
            tcpSocket:
              port: http
            failureThreshold: 18
            periodSeconds: 5
          volumeMounts:
            - name: config
              mountPath: /etc/stock-trading/config
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: config
          configMap:
            name: user-service-config
            items:
              - key: config.yaml
                path: config.yaml
        - name: tmp
          emptyDir: {}
