piVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: analytics-service
  ports:
    - name: http
      port: 80
      targetPort: 8080