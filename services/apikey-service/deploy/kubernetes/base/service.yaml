piVersion: v1
kind: Service
metadata:
  name: apikey-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: apikey-service
  ports:
    - name: http
      port: 80
      targetPort: 8080