piVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: audit-service
  ports:
    - name: http
      port: 80
      targetPort: 8080