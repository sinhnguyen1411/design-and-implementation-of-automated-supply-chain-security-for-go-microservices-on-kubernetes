piVersion: v1
kind: Service
metadata:
  name: backtest-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: backtest-service
  ports:
    - name: http
      port: 80
      targetPort: 8080