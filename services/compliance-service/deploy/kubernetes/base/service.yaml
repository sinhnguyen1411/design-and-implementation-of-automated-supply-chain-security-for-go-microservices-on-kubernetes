apiVersion: v1
kind: Service
metadata:
  name: compliance-service
  namespace: stock-trading
  labels:
    app.kubernetes.io/name: compliance-service
spec:
  selector:
    app.kubernetes.io/name: compliance-service
  ports:
    - port: 80
      targetPort: 8080
