apiVersion: v1
kind: Service
metadata:
  name: execution-service
  namespace: stock-trading
  labels:
    app.kubernetes.io/name: execution-service
spec:
  selector:
    app.kubernetes.io/name: execution-service
  ports:
    - port: 80
      targetPort: 8080
