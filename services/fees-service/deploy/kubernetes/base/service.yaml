piVersion: v1
kind: Service
metadata:
  name: fees-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: fees-service
  ports:
    - name: http
      port: 80
      targetPort: 8080