piVersion: v1
kind: Service
metadata:
  name: margin-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: margin-service
  ports:
    - name: http
      port: 80
      targetPort: 8080