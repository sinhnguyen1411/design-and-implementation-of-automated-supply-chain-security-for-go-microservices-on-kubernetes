apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: stock-trading
  labels:
    app.kubernetes.io/name: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: notification-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: notification-service
      annotations:
        scs.gate/sbom-digest: "sha256:placeholder"
        scs.gate/critical-count: "0"
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/sinhnguyen1411/stock-trading/
          ports:
            - containerPort: 8080
