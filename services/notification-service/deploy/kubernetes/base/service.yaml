apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: stock-trading
  labels:
    app.kubernetes.io/name: notification-service
spec:
  selector:
    app.kubernetes.io/name: notification-service
  ports:
    - port: 80
      targetPort: 8080
