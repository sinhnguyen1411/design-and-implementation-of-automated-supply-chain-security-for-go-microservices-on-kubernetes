apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: portfolio-service
  ports:
    - name: http
      port: 80
      targetPort: 8080