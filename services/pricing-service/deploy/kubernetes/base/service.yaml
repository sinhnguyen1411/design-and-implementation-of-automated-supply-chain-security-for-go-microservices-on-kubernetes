apiVersion: v1
kind: Service
metadata:
  name: pricing-service
  namespace: stock-trading
  labels:
    app.kubernetes.io/name: pricing-service
spec:
  selector:
    app.kubernetes.io/name: pricing-service
  ports:
    - port: 80
      targetPort: 8080
