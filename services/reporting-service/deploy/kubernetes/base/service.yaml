piVersion: v1
kind: Service
metadata:
  name: reporting-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: reporting-service
  ports:
    - name: http
      port: 80
      targetPort: 8080