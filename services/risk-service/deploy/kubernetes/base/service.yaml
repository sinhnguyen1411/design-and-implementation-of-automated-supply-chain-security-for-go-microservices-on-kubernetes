apiVersion: v1
kind: Service
metadata:
  name: risk-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: risk-service
  ports:
    - name: http
      port: 80
      targetPort: 8080