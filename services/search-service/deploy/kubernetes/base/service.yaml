piVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: stock-trading
spec:
  selector:
    app.kubernetes.io/name: search-service
  ports:
    - name: http
      port: 80
      targetPort: 8080