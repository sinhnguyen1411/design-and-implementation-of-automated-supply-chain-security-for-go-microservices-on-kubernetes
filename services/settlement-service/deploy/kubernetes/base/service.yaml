apiVersion: v1
kind: Service
metadata:
  name: settlement-service
  namespace: stock-trading
  labels:
    app.kubernetes.io/name: settlement-service
spec:
  selector:
    app.kubernetes.io/name: settlement-service
  ports:
    - port: 80
      targetPort: 8080
